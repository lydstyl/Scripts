#!/bin/bash
#cd ~/dev05/bash && gulp

cd ~/babyliss/conair-uk && ls &&
gnome-terminal -x 'gulp' &&
gnome-terminal -x 'htop' &&
gnome-terminal -x '~/.dropbox-dist/dropboxd'
# ~/eclipse/jee-neon/eclipse/eclipse
# ~/.dropbox-dist/dropboxd
# gnome-terminal -x 'code'